# WSxx — title

## Objective

State the single decision or deliverable this chat owns.

## Why this is separate

State the confound or repository-overlap avoided by isolating it.

## Ownership

List exact writable paths, read-only dependencies, and forbidden paths.

## Requirements and assumptions

Separate non-negotiable requirements from assumptions that may be challenged. Reference
canonical claim, assumption, reviewer-question, and figure IDs.

## Tasks and deliverables

Use bounded tasks. Each deliverable must have a path and an acceptance check.

## Stop conditions

State which missing decision or failed check requires handback rather than improvisation.

## Starting context

List the minimum files and known findings needed to work without rereading the repository.

## Paste-ready prompt

Include the complete prompt for a new chat, with scope, constraints, expected output, and
the instruction not to edit `research/registry.yaml` or generated `research/STATUS.md`.
