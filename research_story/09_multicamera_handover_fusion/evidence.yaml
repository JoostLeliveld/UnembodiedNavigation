chapter: "09"
status: PLUMBING
world: full_4cam
contribution: candidate_second_contribution_B
gate: "no fusion claims until per-camera detector chains + D2 pass (>=30 synchronized pairs, <=10% outliers) + replay baselines + closed-loop handover campaign; active planner handover stays disabled"
conditions: "S0-S4 baselines + M8 hysteretic manager (implemented); plan IDs M0-M7 map onto these; oracle selection = evaluation-only"
evidence:
  - path: ../../src/reliability
    kind: runtime_code
    tag: established_code
    note: "camera contracts, health, M8 CameraManager (operational-only), handover covariance inflation; WIP on multicam branch"
  - path: ../../tests/reliability
    kind: tests
    tag: established_code
  - path: ../../experiments/multicamera_fusion_extension
    kind: study
    tag: model_plumbing
  - path: ../../experiments/multicamera_commissioning_bigwarehouse
    kind: study
    tag: measured_in_sim
    note: "Full_4cam operational pipeline pilot exists (two routes, four posterior fits, three strict C/D pairs); it is not qualified mapping/fusion/closed-loop evidence. The frozen protocol and analysis plan require D0 calibration, route-disjoint D1, all D2 edges, matched manifest-backed D3 replay, and D4 active confirmation. Historical retired-testbed pilot remains provenance only."
  - path: ../../logs/studies/calibration_drift_lifecycle/exp1_stale_correction
    kind: study_output
    tag: measured_in_sim
    note: "C5 -> lifecycle (ICRA addendum C3). On the capture HELD OUT of the v3 fit, with
      drift injected geometrically (A4/A8) into real recorded pixels: (1) a STALE 2-DOF bias
      correction becomes HARMFUL at 0.25 deg yaw on camera C, the camera it helps most
      (0.043 -> 0.100 m vs 0.094 m uncorrected), so lifecycle risk scales with correction
      size and falls only on CALIBRATE cameras; (2) the commissioning gate CANNOT be reused
      as the in-service monitor -- absolute |b_cross|/sigma fires at rest on A (10.2) and B
      (5.0) vs threshold 1.2, and is MASKED on B at 0.25 deg (ratio falls to 0.31) when the
      induced bias cancels the resident one; (3) the CHANGE form
      |b_cross(d)-b_cross(0)|/sigma(0) is monotone on all 4 cameras x both fault types and
      detects at 0.1 deg / 0.025 m, one rung BEFORE harm. Identity drift reproduces the
      deployed projection to 0.00e+00 m over 230 detections. GT eval-only (oracle RMS)."
figures:
  planned: [09A, 09B, 09C, 09D, 09E, 09F]
  video: {V09A: planned, V09B: planned, V09C: planned}
