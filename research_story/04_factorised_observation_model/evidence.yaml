chapter: "04"
status: PLANNED
world: aws_1cam_then_full_4cam
contribution: candidate_second_contribution_A
gate: "promote only if: both components learned + independently validated + beats scalar trust + measurably changes R_plan/navigation"
depends_on: ["02 (Fig 02D outcome)", "03 (frozen GP machinery)"]
evidence:
  - path: ../../logs/studies/operational_residual_rcond/exp2_operational_rcond
    kind: study_output
    tag: open
    note: "First OPERATIONAL (GT-free) R_cond estimate: residual referenced to a smoothed
      belief instead of truth, state contribution subtracted via C = H P^s H^T + R_cond.
      Oracle path reproduces the published per-camera sigma table (A .031 B .012 C .035
      D .028) exactly, and the operational estimate lands within ~10% for A/C/D and
      recovers camera C's +0.078m lateral bias (0.084 vs 0.077). BUT: per-camera R_cond
      LOSES to a constant pooled matrix on held-out MNLL (-0.40 vs -1.62) and coverage@95
      is 0.58 vs nominal 0.95, so the conditional-accuracy half is NOT yet promotable.
      New measured constraint: the anchored (non-held-out) estimate understates camera A
      by 4.2x -- an R_cond fitted against a trajectory its own camera anchored is not a
      measurement."
  - path: ../../experiments/operational_residual_rcond/PLAN.md
    kind: doc
    tag: open
    note: "Adapted plan; records which milestones of the incoming greenfield spec were cut
      as already-built or already-answered (notably p_use: the GP null vs FOV/range stands)."
figures:
  planned: [04A, 04B, 04C, 04D]
  video: {V04: planned}
