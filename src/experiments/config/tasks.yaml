tasks:
  warehouse_occ_light.world.sdf:
    - name: main_shadow_tradeoff
      description: Main benchmark task. Starts on the lower northwest approach and asks the planner to reach the southeast goal, forcing a choice between the shorter occluded crossover and the longer clearer route.
      tier: benchmark
      claim: shadow_tradeoff
      focus: visibility_tradeoff
      start: {x: -2.0, y: 0.5, z: 0.05, yaw: 0.0}
      goal:  {x: 1.55, y: -1.45}
