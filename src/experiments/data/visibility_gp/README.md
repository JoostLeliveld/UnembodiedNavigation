# `experiments/data/visibility_gp`

This folder is reserved for packaged compatibility GP artifacts. It currently
does not contain the canonical current output.

## Active Artifact Location

The current GP artifact is:

- a fitted GP `.npz` (the previous curated artifact was retired with the 2026-08-25 clean sheet)

## Rule

Comparison runs in the current path must pass `visibility_artifact_path` explicitly. These packaged files should not be presented as the fitted current artifact unless the campaign config and docs explicitly say so.
