"""Optional live ROS 2 wrappers around the reliability library.

The library itself stays ROS-independent; everything under this package
soft-imports ``rclpy`` so non-ROS tooling can keep importing ``reliability``.
"""
