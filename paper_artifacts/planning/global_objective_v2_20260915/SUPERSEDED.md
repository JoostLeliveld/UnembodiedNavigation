# Superseded

This directory is the first offline validation run of the corrected global route
objective. It is kept as evidence and is **not** the reference result.

Use `../global_objective_v2_20260915b/` instead: same grid, same inputs and the
same selections, plus the "what actually drove the legacy choice" analysis, the
deployed-solve checks, the minimal reproduction transcript and `frozen_routes.json`.

See `docs/global_planner_objective_correction.md`.
