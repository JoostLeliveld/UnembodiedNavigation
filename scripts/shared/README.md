# Shared analysis helpers

- `metrics.py` is the canonical implementation of scoring metrics used by current analyses.
- `paths.py` locates the repository root.
- `experiments/fusion_on_fixed_routes/aligned.py` is the canonical reader for final
  navigation logs and follows `docs/localization_metrics.md`.

Do not copy old metric implementations or recover conclusions from archived result files.
